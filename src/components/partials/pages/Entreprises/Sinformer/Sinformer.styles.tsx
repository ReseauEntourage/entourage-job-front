import styled from 'styled-components';
import { COLORS } from 'src/constants/styles';

export const StyledHowToBeInclusiveTitleContainer = styled.div`
  padding-left: 0.7rem;
  border-left: 2px solid ${COLORS.primaryBlue};
`;
