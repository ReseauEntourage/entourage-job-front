import { Container } from './Container';
import { Item } from './Item';
import {
  StyledLine as Line,
  StyledItemContainer as ItemGroup,
} from './TimeLineHorizontal.styles';

// Exporting as an object to allow for future expansion of the component
export const TimeLineHorizontal = {
  Container,
  Item,
  Line,
  ItemGroup,
};
