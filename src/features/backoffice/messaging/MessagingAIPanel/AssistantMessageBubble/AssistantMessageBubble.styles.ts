import { styled } from 'styled-components';

// Live region (role="status" set by the component) wrapping the notice shown
// under a truncated or interrupted answer.
export const StyledAssistantMessageNotice = styled.div`
  margin-top: 4px;
`;
