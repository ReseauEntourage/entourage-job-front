import styled from 'styled-components';

export const StyledDirectoryFilters = styled.div`
  display: flex;
  flex-direction: column;
  gap: 16px;
`;

export const StyledDirectoryButtonContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
`;

export const StyledDirectoryOrderBy = styled.div`
  display: flex;
  justify-content: flex-end;
  align-items: center;
  gap: 12px;
`;

export const StyledDirectoryOrderByButtonsContainer = styled.div`
  display: flex;
  gap: 8px;
`;

export const StyledSeparator = styled.hr`
  width: 100%;
  margin: 5px 0;
`;
