// Délai au-delà duquel l'utilisateur peut basculer vers la saisie manuelle
// pendant l'étape cv-loading (génération de profil à partir du CV).
export const CV_GENERATION_TIMEOUT_MS = 40000;

// Durée estimée de la génération de profil, utilisée pour espacer l'affichage
// des messages de chargement afin que chacun n'apparaisse qu'une seule fois.
export const CV_GENERATION_ESTIMATED_DURATION_MS = 15000;
